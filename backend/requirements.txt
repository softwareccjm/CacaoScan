# Dependencias directas. Las transitivas las resuelve pip.
# Si necesitas un lock reproducible exacto, genera uno aparte con `pip freeze > requirements.lock`.

# --- Django y REST ---
Django==5.2.9
djangorestframework==3.16.1
djangorestframework_simplejwt==5.5.1
django-cors-headers==4.9.0
django-filter==25.2
django-redis==6.0.0
django-storages==1.14.6
drf-yasg==1.21.7

# --- Realtime y tareas async ---
channels==4.3.2
channels_redis==4.3.0
celery==5.6.0
redis==7.1.0

# --- Servidor y estáticos ---
gunicorn==23.0.0
whitenoise==6.8.2

# --- Base de datos y config ---
psycopg2-binary==2.9.11
python-dotenv==1.2.1

# --- Integraciones externas ---
google-auth==2.36.0
sendgrid==6.11.0
requests==2.32.5

# --- ML / Visión ---
# torch/torchvision se instalan aparte en el Dockerfile desde el índice CPU de PyTorch.
# Mantén estas líneas comentadas para que pip no las baje del índice por defecto (CUDA, ~2 GB).
# torch==2.5.1
# torchvision==0.20.1
timm==0.9.12
ultralytics==8.3.234
opencv-python-headless==4.12.0.88
pillow==12.0.0
numpy==2.1.3
pandas==2.3.3
scipy==1.16.2
scikit-learn==1.7.2
scikit-image==0.25.2
matplotlib==3.10.7
seaborn==0.13.2

# --- Reportes ---
reportlab==4.0.4
openpyxl==3.1.5
XlsxWriter==3.1.9

# --- YAML (configs ML) ---
PyYAML==6.0.3

# --- Tests ---
pytest==9.0.1
pytest-cov==7.0.0
pytest-django==4.11.1
pytest-xdist==3.8.0
coverage==7.12.0
